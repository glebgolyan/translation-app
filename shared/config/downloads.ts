// shared/config/downloads.ts
//
// Direct download URLs for the TranslateOS desktop app (see the separate
// translation-desktop repo). Update these once the installers are uploaded
// somewhere with a stable public link — nothing else needs to change.
export const DESKTOP_APP_DOWNLOADS = {
  mac: 'https://REPLACE_ME/TranslateOS-mac.dmg',
  windows: 'https://REPLACE_ME/TranslateOS-Setup-windows.exe',
};
